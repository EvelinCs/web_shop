export class User {
    userId: string;
    userEmail: string;
    userName: string;
    userAddress: string;
    userPhone: string;

    constructor(userId: string, userEmail: string, userName: string, userAddress: string, userPhone: string ){
        
    }
}